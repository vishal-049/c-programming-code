#include <stdio.h>

int main()
{
    int num;
    scanf("%d", &num);

    if (num >=18)          
        printf("eligible to vote");
    
    else
        printf("not elogible for vote");

    return 0;
}
