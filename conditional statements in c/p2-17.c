#include <stdio.h>

int main()
{
    int a;
    scanf("%d", &a);
    if (a>=10 && a<=20)
    printf("number is in the range");

    else           
        printf("number is not in the range");
    
    
   

    return 0;
}
